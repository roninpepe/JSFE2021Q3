import { EventCallback, EventHandler } from '../types/utils';

function $(selector: string, scope: HTMLElement | Document = document): HTMLElement {
  return <HTMLElement>scope.querySelector(selector);
}

export function EventCallbackBuilder(callback: EventCallback, className?: string): EventHandler {
  const handler: EventHandler = {
    handleEvent(event: Event) {
      if (className) {
        if (event.target && (<Element>event.target).classList.contains(className)) {
          callback(event);
        }
      } else {
        callback(event);
      }
    },
  };
  return handler;
}

export function toggleArrayItem<T>(array: Array<T>, value: T): void {
  const index: number = array.indexOf(value);
  if (index === -1) {
    array.push(value);
  } else {
    array.splice(index, 1);
  }
}

export function selfAssessment(): void {
  console.log(`Score: 190/190

  ## Functional requirements
  - [x] Basic structure: (25)
    - [x] There should be two views on the site: "Garage" and "Winners". (5)
    - [x] "Garage" view should contain its name, page number, and the full amount of items in the database (how many car user has in his garage). (5)
    - [x] "Winners" view should contain its name, page number, and the full amount of items in the database (how many records the winners table contains). (5)
    - [x] View state should be saved when user switches from one view to another. For example, page number shouldn't be reset, input controls should contain that they contained before switching, etc. (10)
  - [x] "Garage" view: (50)
    - [x] User should be able to create, update, delete a car, and see the list of the cars. Car has only two attributes: "name" and "color". For "delete"-operation car should be deleted from "garage" table as well as from "winners". (15)
    - [x] User should be able to select any color from an RGB-Palete and see the picture of the car colored with the color selected and car's name. (10)
    - [x] Near the car's picture should be buttons to update its attributes or delete it. (5)
    - [x] There should be pagination on the "Garage" view (7 cars per one page). (10)
    - [x] There should be a button to create random cars (100 cars per click). Name should be assembled from two random parts, for example "Tesla" + "Model S", or "Ford" + "Mustang" (At least 10 different names for each part). Color should be also generated randomly. (10)
  - [x] Car animation: (50)
    - [x] Near the car's picture should be buttons for starting / stoping the car engine. (5)
    - [x] User clicks to the engine start button -> UI is waiting for car's velocity answer -> animate the car and makes another request to drive. In case api returned 500 error car animation should be stopped. (20)
    - [x] User clicks to the engine stop button -> UI is waiting for answer for stopping engine -> car returned to it's initial place. (5)
    - [x] Start engine button should be disabled in case car is already in driving mode. As well as stop engine button should be disabled when car is on it's initial place. (5)
    - [x] Car animation should work fine on any screen (smallest screen size is 500px). (15)
  - [x] Race animation: (30)
    - [x] There should be a button to start race. After user clicks this button all the cars on the current page start driving. (10)
    - [x] There should be a button to reset race. After user clicks this button all the cars return to it's initial places. (10)
    - [x] After some car finishes first user should see the message contains car's name that shows which one has won. (10)
  - [x] "Winners" view: (35)
    - [x] After some car wins it should be displayed at the "Winners view" table. (10)
    - [x] There should be pagination (10 winners per one page). (10)
    - [x] Table should include the next culumns: "№", "Image of the car", "Name of the car", "Wins number", "Best time in seconds" (names of the columns can differ). If the same car wins more than once the number of wins should be incremented while best time should be saved only if it's better than the stored one. (10)
    - [x] User should be able to sort cars by wins number and by best time (ASC, DESC). (10)
  
  ================================================================
  
  Score: 150/150
  
  ## Non-functional requirements
  - [x] Application is divided to logical modules / layers. Like working with api it's separate module, as well as working with UI rendering, or working with an application state, etc. Better discuss architecture with a mentor before implementing. (40)
  - [x] All the HTML-content is generated by JavaScript (body contains only one tag script inside). (30)
  - [x] Application is SPA. (25)
  - [x] Webpack or another bundler should be used. There should be one HTML-file, JS-file, and one CSS-file (or it can be embedded right in the HTML-file). (20)
  - [x] Eslint with Airbnb style guide(https://github.com/airbnb/javascript) should be used. Some paricular eslint-rules can be disabled or changed only after agrement with a mentor. There shouldn't be any eslint errors or warnings. (15)
  - [x] Code divided to small functions with a clear names and purposes. Each function should be less or equal to 40 lines. (10)
  - [x] There are not unnecessary code duplications. (5)
  - [x] Code shouldn't contain magical numbers or strings. (5)`);
}

export function logP<T>(promise: Promise<T>): void {
  promise.then((p) => {
    console.log(p);
  });
}

export function getRandomNum(num: number): number {
  return Math.floor(Math.random() * num);
}

export default $;
